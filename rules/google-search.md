---
alwaysApply: true
---

Prefer `google_search` over built-in web search for research, current events, documentation, and anything where citations add value.

Falls back to built-in web search for quick lookups or if google_search is unavailable.

Do not retry on rate limit — the server handles retries automatically.
